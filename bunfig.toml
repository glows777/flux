[test]
# 使用 happy-dom 作为测试环境
preload = ["./test-setup.ts"]

# 只运行 __tests__ 目录下的测试
root = "./__tests__"

# 覆盖率配置
coverage = true
coverageDir = "coverage"
coverageReporter = ["text", "lcov"]
coverageThreshold = { line = 80, function = 80, statement = 80 }
