/**
 * Shared constants for AI module.
 *
 * Kept in a separate file so client components can import
 * without pulling in server-only dependencies (Prisma, pg, etc.).
 */

export const TRUNCATE_LIMIT = 20
